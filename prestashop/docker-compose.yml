version: '2'
services:
  db:
    image: mysql:5.7
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: admin
    ports:
      - 3307:3306
  presta:
    image: prestashop/prestashop
    restart: unless-stopped
    links:
      - db:db
    volumes:
      - //c/Users/winadmin/workspace/container_storage/pslast/:/var/www/html
    ports:
      - 8080:80
    env_file:
      - prestashop.env
    depends_on:
      - db
